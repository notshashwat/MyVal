var Buffer = require('buffer').Buffer
global.window.parseEmail = parse
global.window.Buffer = Buffer
var Require=require('dotenv').config();

var nearestDate = require('nearest-date');
global.window.nearestDate = nearestDate
